apiVersion: v1
kind: Service
metadata:
  name: quest-tracker-api
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: quest-tracker-api